# Help Wanted — isVisible

Deeper tracks needing a second pair of hands — not good-first, but mentored.

- **Braille tables:** Spanish/Portuguese `liblouis` tables + 8-dot computer braille toggle (`liblouisAdapter.ts`, `RESEARCH.md`).
- **NVDA/HID spike:** One of USB HID descriptor *or* NVDA add-on spike — keep relay honesty (`HARDWARE.md`, `TACTILE_PLUGIN.md`).
- **Graphics pilot:** N=5 study for `TactileGraphicsPage` region labels + scale legend (`FIELD_STUDY_PROTOCOL.md`).
- **Coverage:** `vitest --coverage` + `ruff --fix` gate (DX, `ci.yml`).
- **Architecture diagram:** Module dependency graph from `pwa/src/App.tsx` lazy graph (ARCHITECTURE.md).

Ownership map: PWA @framesxsab, tactile @framesxsab, firmware @framesxsab, docs/research @framesxsab — see `CODEOWNERS`. PRs welcome; keep BRLTTY as relay.
