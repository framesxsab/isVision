# Contributing Advanced — isVisible

Beyond `CONTRIBUTING.md` 5-min path.

- **Plugin:** `PLUGIN_GUIDE.md` + `SDK.md` + `docs/PLUGIN_ARCHITECTURE.md` — no core touching needed
- **A11y:** `ACCESSIBILITY.md` + `headingValidator` + `LiveRegion` — every PR must keep `npm run verify` green (build 212 + 32 python + axe 83)
- **Research:** `FIELD_STUDY_PROTOCOL.md` + `REPRODUCIBILITY.md` — anonymize before commit, no fabricated data
- **Release:** `CHANGELOG.md` Keep-a-Changelog, `MIGRATION_PLUGIN.md`, `release.yml` tag → build → artifact
